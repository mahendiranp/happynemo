import { combineReducers } from 'redux';


const signupContactDetails = (state = [], action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default combineReducers({
  signup: signupContactDetails,
});